{
  "name": "paint-svg",
  "version": "1.0.0",
  "description": "Paint with an svg",
  "private": true,
  "scripts": {
    "start": "ruby -run -e httpd . -p 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "svg",
    "potrace",
    "color"
  ],
  "author": "gmfholley",
  "license": "GPL-2.0",
  "dependencies": {
    "potrace-wasm": "^1.0.4",
    "vanilla-picker": "^2.10.1"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "svgstore-cli": "^1.3.1"
  }
}
